
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/snapshots:/app/snapshots
      - ./backend/database.db:/app/database.db

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
